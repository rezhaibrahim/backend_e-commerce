const router = require('express').Router()
const conditionController = require('../../controllers/condition')

// router.get('/', conditionController.getProducts) // show new product
// router.get('/:id', conditionController.detailProduct) // show detail product

module.exports = router
